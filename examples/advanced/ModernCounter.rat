# Counter with increment/decrement operators
# Demonstrates pre/post increment and decrement

proc ModernCounter(initial) {
    state: initial,
    receive {
        | :preInc -> ++state
        | :postInc -> state++
        | :preDec -> --state
        | :postDec -> state--
        | {:get, sender} -> sender <- state
    }
}

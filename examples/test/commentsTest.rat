proc main() {
// Test des commentaires ligne
/* Test des 
   commentaires
   multi-lignes */

// Test des arrays
let arr = [1, 2, 3, 4, 5]
let first = arr[0]  // Premier élément
let last = arr[4]   /* Dernier élément */
print(first)
print(last)

/* 
 * Test des floats
 */
let x = 3.14
let y = 2.0
let sum = x + y
let product = x * y
print(sum)
print(product)
}

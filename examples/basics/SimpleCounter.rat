proc Counter(initial) {
    state: initial,
    receive {
        | :increment -> state = state + 1
        | :decrement -> state = state - 1
        | :reset -> state = 0
    }
}

let g = spawn Counter(0)
g <- :increment
g <- :increment
print(g)